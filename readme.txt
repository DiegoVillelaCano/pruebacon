para correr el backend
npm install -g nodemon
npm install cors --save
npm install express
nodemon ./server.js  

Para ser honesto termine el backend desde las 5:00 aproximadamente pero se me creo un relajo en el frontend usando react entonces no lo pude concluir(parece que no era tan parecido a Angular como creía).
